---
import Layout from "../layouts/Layout.astro"
import Hero from "../components/Hero.astro";
import CTA from "../components/CTA.astro";
import AsideCards from "../components/AsideCards.astro";

export interface Props {
    title: String,
    bgImg: String,
    heading: String,
    tagline: String,
    mainHeading: String,
}

const { title, bgImg, heading, tagline, mainHeading } = Astro.props;
---

<Layout title={`${title} | Freeway Mobile Tyres`}>
    <Hero bgImg={bgImg} heading={heading} tagline={tagline} />
    <main class="py-5">
        <div class="container">
            <div class="row">
                <div class="col-12 col-lg-8">
                    <h1 class="text-center text-lg-start">{mainHeading}</h1>
                    <slot />
                </div>
                <div class="col-12 col-lg-4">
                    <AsideCards />
                </div>
            </div>
        </div>
    </main>
    <CTA />
</Layout>